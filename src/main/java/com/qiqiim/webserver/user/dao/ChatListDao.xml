<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qiqiim.webserver.user.dao.ChatListDao">

	<select id="selectHistoryChat" resultType="com.qiqiim.constant.ChatEntity">
		select * from messages where user_id = #{user_id} and another_id = #{another_id} or user_id = #{another_id} and another_id = #{user_id}
	</select>

</mapper>