<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qiqiim.webserver.user.dao.ChatListDao">

	<select id="selectHistoryMsg" resultType="com.qiqiim.constant.ChatEntity">
		select * from messages where sendid = #{sendid} and receiveid = #{receiveid} or sendid = #{receiveid} and receiveid = #{sendid}
	</select>

	<insert id="insertMsg" useGeneratedKeys="true" keyProperty="id" parameterType="com.qiqiim.constant.ChatEntity">
		insert into messages (sendid,receiveid,content,time) values(#{senderId},#{receiverId},#{content},CURRENT_TIMESTAMP)
		insert into messages (sendid,receiveid,content,time) values(#{receiverId},#{senderId},#{content},CURRENT_TIMESTAMP)
	</insert>
	
	<delete id="deleteMsg">
		delete from messages where sendid = {#sendid} and receiveid = #{receiveid}
	</delete>
</mapper>