<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qiqiim.webserver.user.dao.ChatListDao">

	<select id="selectHistoryChat" resultType="com.qiqiim.constant.ChatEntity">
		select * from messages where userId = #{userId} and anotherId = #{anotherId} or userId = #{anotherId} and anotherId = #{userId} order by time desc
	</select>

</mapper>