<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qiqiim.webserver.user.dao.MsgListDao">

	<select id="queryMessageList" resultType="com.qiqiim.constant.Message">
		select a.*,b.name from msg_list a,user b where user_id = #{value} and a.user_id = b.id
	</select>

	<insert id="insertMessage" parameterType="com.qiqiim.constant.ChatEntity">
		insert into msg_list (user_id,another_id,content,unread,time) values (#{senderId},#{receiverId},#{content},1,CURRENT_TIMESTAMP) on duplicate key update content=#{content},unread=unread+1,time=CURRENT_TIMESTAMP
		insert into msg_list (user_id,another_id,content,unread,time) values (#{receiverId},#{senderId},#{content},1,CURRENT_TIMESTAMP) on duplicate key update content=#{content},unread=unread+1,time=CURRENT_TIMESTAMP
		insert into messages (user_id,another_id,content,time) values(#{user_id},#{another_id},#{content},CURRENT_TIMESTAMP)
		insert into messages (user_id,another_id,content,time) values(#{another_id},#{user_id},#{content},CURRENT_TIMESTAMP)
	</insert>
	
	<delete id="deleteMessage">
		delete from messages where user_id = {#user_id} and another_id = #{another_id}
	</delete>
	
	<update id="readMessage">
		update msg_list set unread = 0 where user_id = #{user_id} and another_id = #{another_id}
	</update>
</mapper>